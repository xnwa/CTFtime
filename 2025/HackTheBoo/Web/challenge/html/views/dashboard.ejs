<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Control Panel - Threshold Monitoring System</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/css/halloween-theme.css">
    <link rel="stylesheet" href="/css/atmospheric-effects.css">
    <link rel="stylesheet" href="/css/animations.css">
</head>
<body class="bg-black overflow-x-hidden">
    <!-- Atmospheric Effects -->
    <div id="fog-layer" class="fog-container"></div>
    <div id="rain-layer" class="rain-container"></div>
    <canvas id="particle-canvas" class="particle-canvas"></canvas>

    <div class="min-h-screen relative z-10">
        <!-- Header -->
        <header class="border-b border-purple-900 bg-black/50 backdrop-blur-sm sticky top-0 z-50">
            <div class="max-w-7xl mx-auto px-4 py-4">
                <div class="flex items-center justify-between">
                    <div class="flex items-center gap-4">
                        <svg class="w-8 h-8 text-orange-500 animate-flicker" fill="currentColor" viewBox="0 0 24 24">
                            <path d="M12 2L9 6h6l-3-4zm0 20l3-4H9l3 4zm0-18v16M8 8h8v8H8V8z"/>
                        </svg>
                        <div>
                            <h1 class="text-xl font-bold text-orange-400">Threshold Monitoring System</h1>
                            <p class="text-purple-400 text-xs">Elin Croft Research Institute</p>
                        </div>
                    </div>
                    <form method="POST" action="/logout" style="margin: 0;">
                        <button type="submit" class="logout-button">
                            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"/>
                            </svg>
                            <span>Logout</span>
                        </button>
                    </form>
                </div>
            </div>
        </header>

        <main class="max-w-7xl mx-auto px-4 py-8">
            <!-- User Info Card -->
            <div class="mystical-card mb-8">
                <div class="card-glow"></div>
                <div class="relative z-10 p-6">
                    <div class="flex items-center justify-between flex-wrap gap-4">
                        <div class="flex items-center gap-4">
                            <div class="w-16 h-16 rounded-full bg-gradient-to-br from-orange-500 to-purple-600 flex items-center justify-center">
                                <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"/>
                                </svg>
                            </div>
                            <div>
                                <h2 class="text-2xl font-bold text-orange-400">
                                    Welcome, <span class="text-purple-300"><%= user.username %></span>
                                </h2>
                                <div class="flex items-center gap-2 mt-1">
                                    <span class="text-purple-400 text-sm">Clearance:</span>
                                    <span class="px-3 py-1 rounded-full bg-purple-900/50 border border-purple-600 text-purple-300 text-sm font-semibold"><%= user.clearance_level %></span>
                                </div>
                            </div>
                        </div>
                        <div class="flex items-center gap-2">
                            <div class="w-3 h-3 rounded-full bg-green-500 animate-pulse"></div>
                            <span class="text-green-400 text-sm">System Online</span>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Classified Data Section -->
            <% if (hasHighAuthority) { %>
            <div class="mystical-card mb-8">
                <div class="card-glow"></div>
                <div class="relative z-10 p-8">
                    <h3 class="text-3xl font-bold text-orange-400 mb-6 text-center">🔐 Classified Research Data 🔐</h3>
                    <div class="bg-black/95 p-6 rounded-xl border-2 border-green-500 text-center">
                        <h4 class="text-green-400 text-xl mb-4">✨ Access Granted ✨</h4>
                        <p class="text-purple-200 mb-6 text-sm">Your authorization level permits access to classified research data. The following information is restricted to personnel with divine authority clearance.</p>
                        <div class="bg-green-900/20 p-6 rounded-lg border border-green-600">
                            <p class="text-green-200 font-mono text-lg font-bold break-all">
                                <%= flag %>
                            </p>
                        </div>
                    </div>
                </div>
            </div>
            <% } else { %>
            <div class="mystical-card mb-8">
                <div class="card-glow"></div>
                <div class="relative z-10 p-8">
                    <h3 class="text-3xl font-bold text-orange-400 mb-6 text-center">🔐 Classified Research Data 🔐</h3>
                    <div class="bg-black/95 p-6 rounded-xl border-2 border-red-600 text-center">
                        <h4 class="text-red-400 text-xl mb-4">🚫 ACCESS DENIED 🚫</h4>
                        <p class="text-purple-200 mb-6 text-sm">Your clearance level is insufficient to access classified research data. Only personnel with guardian role and divine authority clearance may access this information.</p>
                        <div class="bg-red-900/20 p-6 rounded-lg border border-red-600">
                            <p class="text-red-300 font-mono text-xl font-bold">
                                403 FORBIDDEN - INSUFFICIENT CLEARANCE
                            </p>
                            <p class="text-orange-400 text-sm mt-3">
                                Required: 'guardian' Role with 'divine_authority' Clearance
                            </p>
                        </div>
                    </div>
                </div>
            </div>
            <% } %>

            <!-- Breach Analysis Section -->
            <div class="mystical-card">
                <div class="card-glow"></div>
                <div class="relative z-10 p-8">
                    <div class="text-center mb-8">
                        <div class="inline-block p-4 rounded-full bg-gradient-to-br from-purple-900 to-black border-2 border-orange-500 mb-4">
                            <svg class="w-10 h-10 text-orange-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 20l-5.447-2.724A1 1 0 013 16.382V5.618a1 1 0 011.447-.894L9 7m0 13l6-3m-6 3V7m6 10l4.553 2.276A1 1 0 0021 18.382V7.618a1 1 0 00-.553-.894L15 4m0 13V4m0 0L9 7"/>
                            </svg>
                        </div>
                        <h3 class="text-3xl font-bold text-orange-400 mb-2">Breach Analysis Tool</h3>
                        <p class="text-purple-300">Analyze external data sources for anomalous activity</p>
                    </div>

                    <form id="breach-analysis-form" class="max-w-2xl mx-auto">
                        <div class="form-group mb-6">
                            <label for="data_source" class="block text-orange-300 font-semibold mb-3 flex items-center gap-2">
                                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13.828 10.172a4 4 0 00-5.656 0l-4 4a4 4 0 105.656 5.656l1.102-1.101m-.758-4.899a4 4 0 005.656 0l4-4a4 4 0 00-5.656-5.656l-1.1 1.1"/>
                                </svg>
                                Data Source URL
                            </label>
                            <input
                                type="url"
                                id="data_source"
                                name="data_source"
                                required
                                class="mystical-input"
                                placeholder="https://example.com/data-endpoint..."
                            />
                            <p class="text-purple-400 text-xs mt-2">Enter the URL of the external data source to analyze</p>
                        </div>

                        <button type="submit" class="portal-button w-full group">
                            <span class="relative z-10 flex items-center justify-center gap-3">
                                <svg class="w-5 h-5 group-hover:rotate-180 transition-transform duration-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/>
                                </svg>
                                <span class="font-bold">Run Analysis</span>
                            </span>
                        </button>
                    </form>

                    <div id="analysis-result" class="mt-8 hidden">
                        <!-- Results will be populated here -->
                    </div>
                </div>
            </div>

        </main>

        <!-- Footer -->
        <footer class="border-t border-purple-900 mt-16 py-8">
            <div class="max-w-7xl mx-auto px-4 text-center">
                <p class="text-purple-400 text-sm">
                    Elin Croft Research Institute • Black Fen Monitoring Station • Est. 1889
                </p>
            </div>
        </footer>
    </div>

    <script src="/js/atmospheric-effects.js"></script>
    <script src="/js/particle-system.js"></script>
    <script>
        // Dashboard handler script integrated
        document.getElementById('breach-analysis-form').addEventListener('submit', function(e) {
            e.preventDefault();

            const dataSource = document.getElementById('data_source').value;
            const resultDiv = document.getElementById('analysis-result');

            resultDiv.style.display = 'block';
            resultDiv.innerHTML = `
                <div class="bg-purple-900/20 p-6 rounded-lg border border-purple-600 text-center">
                    <p class="text-purple-300 mb-3">Analyzing data source...</p>
                    <div class="inline-block w-8 h-8 border-4 border-purple-600 border-t-orange-500 rounded-full animate-spin"></div>
                </div>
            `;

            fetch('/api/analyze-breach', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ data_source: dataSource })
            })
            .then(response => response.json())
            .then(data => {
                if (data.status === 'success') {
                    // Escape HTML to prevent XSS but display as text
                    const escapedData = data.data
                        .replace(/&/g, '&amp;')
                        .replace(/</g, '&lt;')
                        .replace(/>/g, '&gt;')
                        .replace(/"/g, '&quot;')
                        .replace(/'/g, '&#039;');

                    resultDiv.innerHTML = `
                        <div class="bg-green-900/20 p-6 rounded-lg border-2 border-green-600">
                            <h4 class="text-green-400 text-xl mb-4 font-bold">✨ Analysis Complete ✨</h4>
                            <div class="bg-black/90 p-4 rounded-lg border border-green-700 overflow-auto" style="max-height: 500px;">
                                <pre class="text-green-200 text-sm font-mono whitespace-pre-wrap break-words">${escapedData}</pre>
                            </div>
                            ${data.truncated ? `
                                <p class="text-orange-300 text-sm mt-3">
                                    ⚠️ Data truncated: ${data.originalSize} bytes → ${data.truncatedSize} bytes
                                </p>
                            ` : ''}
                            <p class="text-purple-400 text-xs mt-2 italic">Source: ${data.source}</p>
                        </div>
                    `;
                } else {
                    resultDiv.innerHTML = `
                        <div class="bg-red-900/20 p-6 rounded-lg border-2 border-red-600 text-center">
                            <h4 class="text-red-400 text-xl mb-2 font-bold">⚠️ Analysis Failed ⚠️</h4>
                            <p class="text-red-300">${data.message}</p>
                        </div>
                    `;
                }
            })
            .catch(error => {
                resultDiv.innerHTML = `
                    <div class="bg-red-900/20 p-6 rounded-lg border-2 border-red-600 text-center">
                        <h4 class="text-red-400 text-xl mb-2 font-bold">💀 Connection Failed 💀</h4>
                        <p class="text-red-300">Error: ${error.message}</p>
                    </div>
                `;
            });
        });
    </script>
</body>
</html>
